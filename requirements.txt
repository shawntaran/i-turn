# Application (UI + backend + HTTP client). No model, no torch, no CUDA:
# the model runs behind AI_BASE_URL — see COLLABORATION.md.
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
pydantic>=2.9
python-dotenv>=1.0
